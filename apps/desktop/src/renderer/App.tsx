export function App() {
  return (
    <main>
      <h1>Space</h1>
      <p>
        Foundation scaffold (M0). Workspace, project, Git, and GitHub screens
        are implemented in later milestones and always read real state via
        the typed IPC contract &mdash; never mock data.
      </p>
    </main>
  );
}

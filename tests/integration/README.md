# Integration tests

Run against temporary real repositories and stubbed isolated service
configuration (spec section 34.2): repository init, first commit, branch
operations, staging hunks, conflict states, multiple worktrees, publish
partial failures via test doubles, multiple workspace Git identities,
multiple GitHub auth contexts, terminal PTY lifecycle.

# End-to-end tests

Use packaged or near-packaged builds (spec section 34.3): first-run scan,
workspace creation, add non-Git folder and initialise, create project,
clone repository, switch workspace while processes run, open terminal and
run Node, commit and push to a disposable test repository, PR/check
workflow in a controlled GitHub test organisation, installer cancellation
and resume, application update flow.

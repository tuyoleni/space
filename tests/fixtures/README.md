# Fixtures

Deterministic test fixtures: generated Git repositories (linear history,
octopus merges, criss-cross history, 100,000-commit history, shallow
clones), sample project manifests, and redaction test vectors (spec
section 12.4, 34.5). No real user data or live credentials.

{
  "name": "@space/security",
  "version": "0.1.0",
  "private": true,
  "description": "IPC sender validation (spec sections 20.3, 22.1, 25).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run src"
  },
  "devDependencies": {
    "electron": "43.1.0"
  }
}

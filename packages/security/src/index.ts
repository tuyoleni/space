/**
 * @space/security
 *
 * Permission/risk policy, the secret redaction registry, and IPC sender
 * validation (spec sections 24-25). This package is the enforcement point
 * for every "MUST NOT" boundary in the threat model — changes here require
 * reviewer approval from an assigned owner (spec section 33).
 *
 * Populated during M2 (secure application foundation).
 */
export {};

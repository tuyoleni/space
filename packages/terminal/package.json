{
  "name": "@space/terminal",
  "version": "0.1.0",
  "private": true,
  "description": "PTY host and terminal protocol (spec section 15).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run src"
  },
  "dependencies": {
    "@space/workspace-runner": "^0.1.0",
    "node-pty": "^1.1.0"
  }
}

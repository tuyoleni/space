/**
 * @space/terminal
 *
 * The PTY host and terminal protocol (spec section 15). Uses node-pty for
 * real shell processes and streams to xterm.js in the renderer via
 * backpressure-aware IPC. Not a simulated command console.
 *
 * Populated during M4 (Phase 3: project and terminal foundation).
 */
export {};

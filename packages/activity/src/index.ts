/**
 * @space/activity
 *
 * The activity event model and aggregation for the 52-week grid and daily
 * detail views (spec section 17). Records only real events from operations
 * and observed repository state — never generated/simulated activity.
 *
 * Populated during M5 (Phase 4: complete local Git).
 */
export {};

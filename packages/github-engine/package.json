{
  "name": "@space/github-engine",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub CLI (gh) adapter and GitHub domain models (spec section 14).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run src"
  },
  "dependencies": {
    "@space/domain": "^0.1.0",
    "@space/git-engine": "^0.1.0",
    "@space/security": "^0.1.0"
  }
}

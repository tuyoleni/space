{
  "name": "@space/environment",
  "version": "0.1.0",
  "private": true,
  "description": "Tool registry, installers, and workspace service adapters (spec section 16).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run src"
  },
  "dependencies": {
    "@space/workspace-runner": "^0.1.0"
  }
}

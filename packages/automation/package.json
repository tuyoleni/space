{
  "name": "@space/automation",
  "version": "0.1.0",
  "private": true,
  "description": "Automation triggers, conditions, and actions (spec section 18).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run src"
  },
  "dependencies": {
    "@space/domain": "^0.1.0",
    "zod": "^4.4.3"
  }
}

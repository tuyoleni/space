{
  "name": "@space/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared renderer components (presentation only, no IPC or business logic).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run src"
  }
}
